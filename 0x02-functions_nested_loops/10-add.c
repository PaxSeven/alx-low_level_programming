#include "main.h"
#include <stdio.h>

/**
 * add -adds two integers and returns the resultsmain - check the code
 *@a: int to be added to b
 *@b: int to be added to a
 * Return: sumsz of a and b
 */
int add(int a, int b)
{
	int c;
	
	c=a+b;
	
	return(c);
}
